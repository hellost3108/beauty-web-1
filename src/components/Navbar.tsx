/*
 * The site-wide navigation is now the Melalogy brand bar (black authority bar,
 * white logo, cherry on the active item). Kept as a re-export so every page
 * that already imports `@/components/Navbar` picks it up unchanged.
 */
export { default } from './melalogy/BrandNav';
